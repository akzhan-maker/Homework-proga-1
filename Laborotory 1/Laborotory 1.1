import csv
with open("names.csv", "w", newline="", encoding="utf-8") as f1:
    writer = csv.writer(f1)
    data = input( )
    names = data.split()
    for name in names:
        writer.writerow([name.capitalize()])
print("-" * 20)
print( )
with open("names.csv", "r", encoding="utf-8") as f1:
    reader = csv.reader(f1)
    for row in reader:
        print(row[0])


